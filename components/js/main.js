// Main initialization file
document.addEventListener('DOMContentLoaded', () => {
    // Initialize all modules
    initTheme();
    initMobileMenu();
    initProductModal();
    initCart();
    initFavorites();
    initCheckout();
    initAuth();
});

